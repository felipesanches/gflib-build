# Dependency cohort map (full library)

Generated by `--cohorts-report` over a google/fonts clone, read-only via `git show` on
the bare mirrors. Verified against `main` at `647e52b1` (2026-06-02) — unchanged by that
day's enrichment merges (they only re-pointed configs on already-buildable families: same
repos, commits, requirements).

> The 1423 scanned are the **buildable** families (a gftools build config + a pinned
> commit). The library has **2013** total families; the other **590** lack a buildable
> config or commit (legacy SFD/VFB sources, `missing_config`, no source block) and are out
> of scope for building.

- **Buildable families scanned:** 1423 (of 2013 total in the library)
- **Distinct dependency cohorts:** 216 (plus `base` = no requirements file, and any `mirror-absent`)
- **Singletons (1-family cohorts):** 140
- **Concentration:** the top 5 cohorts cover 923 of 1423 buildable families (~65%).

## Largest cohorts

| Families | Cohort | Representative requirements |
|---:|---|---|
| 467 | base (none) | — |
| 199 | `c-21877ddc0968` | bumpfontversion, chevron, drawbot-skia>=0.4.8, git+https://github.com/notofonts/notobuilde |
| 108 | `c-18e328b000ec` | gftools |
| 104 | `c-e59e22a4e648` | Brotli==1.1.0, Deprecated==1.2.14, GitPython==3.1.37, Jinja2==3.1.2 … (+134) |
| 45 | `c-a673faf1c8ff` | fontbakery>=0.7, fontmake>=2.4, gftools>=0.7 |
| 43 | `c-dfb287733f30` | drawbot-skia>=0.4.8, fontbakery>=0.8, fontmake>=2.4, gftools[qa]>=0.7 … (+1) |
| 35 | `c-bc37a038235a` | -r requirements.in |
| 26 | `c-7ce30120d281` | brotli, fontbakery, gftools |
| 21 | `c-d8716f855b10` | Brotli==1.0.9, Deprecated==1.2.13, GitPython==3.1.27, Jinja2==3.1.2 … (+99) |
| 17 | `c-d0793752f5ca` | bumpfontversion>=0.2.0, drawbot-skia>=0.4.8, fontbakery>=0.8, fontmake>=2.4 … (+2) |
| 9 | `c-3ac364db600b` | Brotli==1.0.9, Deprecated==1.2.14, GitPython==3.1.43, Jinja2==3.1.2 … (+132) |
| 8 | `c-02618213acce` | appdirs==1.4.4, attrs==20.3.0, booleanoperations==0.9.0, brotli==1.0.9 … (+14) |
| 7 | mirror-absent | — |
| 6 | `c-8436552ce41a` | brotli, fontbakery, fontmake, gftools |
| 6 | `c-c2cbc2e5daf5` | brotli, fontbakery, fontmake, gftools … (+1) |
| 6 | `c-03ac81a35e84` | fontbakery[googlefonts]==0.12.6, fontmake==3.9.0, gftools==0.9.54, nanoemoji==0.15.1 … (+1 |
| 5 | `c-b891ab64b985` | Brotli==1.0.9, Deprecated==1.2.13, GitPython==3.1.31, Jinja2==3.1.2 … (+117) |
| 5 | `c-eccd8a38d361` | absl-py==2.1.0, afdko==4.0.1, appdirs==1.4.4, attrs==24.2.0 … (+132) |
| 5 | `c-31ba03d1797d` | git+https://github.com/googlefonts/gftools |
| 5 | `c-516a7ac32cf5` | fontmake==2.0.3, git+https://github.com/googlefonts/gftools |

## How to read this

Each non-`base` cohort becomes **one shared virtualenv** under `--build-dir/venvs/<key>/` when you build with `--manage-venvs`. Families in `base` (no `requirements.txt`) share the base venv. So a full build creates on the order of ~216 venvs rather than one per family — and you can shrink that further by not building the long tail of singletons.

Full machine-readable data (every cohort → its requirements + complete family list) is produced alongside this file as `cohorts.json` in the `--build-dir`.

Regenerate with:

```sh
python3 gflib_build.py --cohorts-report \
  --google-fonts <clone> --archive <repo_archive> --build-dir <out> --jobs 16
```

