__pycache__/
*.pyc
# runtime build artifacts (never committed): outputs, venvs, caches, state, logs
work/
out/
logs/
venvs/
pip-cache/
state.json
*.log
*.tmp

# tool data dir (auto-clones, archive, build outputs) + runtime files — never committed
gflib-data/
build/
status.json
control.json
control.*.tmp
status.json.tmp
timings.json
cohorts.json
events.jsonl
daemon.pid
daemon.log
gflib-build.config
