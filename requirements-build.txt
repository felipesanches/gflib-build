# Base build toolchain (the "base" dependency cohort) for gflib-build.
# Pinned to the gfonts_agents build_registry baseline so results are comparable to the
# existing reproducible-build run (most diffs then categorize as "compiler-version").
#
# SELF-HEALING: you do NOT need to hand-manage these pins. If any pinned version is
# unavailable on PyPI (e.g. a stale setuptools_scm dev pin like the old `gftools==0.9.100.dev4`,
# which never existed on the index), gflib-build's venv installer automatically drops just
# that pin and lets pip backtrack to a compatible version — and records it. Valid pins are
# kept as-is, so reproducibility holds for everything that resolves.
# gftools is pinned to the published tag 0.9.99. To reproduce a specific dev build instead,
# install from git, e.g.:  gftools @ git+https://github.com/googlefonts/gftools@<commit>
fontmake==3.11.1
fonttools==4.61.1
glyphsLib==6.12.7
ufo2ft==3.7.0
ttfautohint-py==0.6.0
gftools==0.9.99
